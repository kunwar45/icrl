# icrl without agentlab / gradio / ray (lighter cluster install for ST-WebAgentBench collection).
# Usage:
#   pip install -e .
#   pip install -r requirements_no_agentlab.txt
#   bash scripts/setup_cluster.sh   # still installs editable BrowserGym + ST-WebAgentBench forks

-r requirements-browser.txt

# ── Core ML stack ─────────────────────────────────────────────────────────────
torch>=2.3.0
transformers>=4.41.0
peft>=0.11.1
trl>=0.9.4
accelerate>=0.30.0
bitsandbytes>=0.43.1
datasets>=2.19.0
huggingface-hub>=0.34.0
safetensors>=0.4.3
tokenizers>=0.21.0
sympy
jinja2
networkx

# ── Scientific / data ─────────────────────────────────────────────────────────
scikit-learn>=1.4.2
scipy>=1.13.0
numpy>=1.26.4
pandas>=2.2.2
matplotlib>=3.9.0
seaborn>=0.13.2
joblib

# ── Config / logging ──────────────────────────────────────────────────────────
omegaconf>=2.3.0
hydra-core>=1.3.2
antlr4-python3-runtime==4.9.*
wandb>=0.17.0
jsonlines>=4.0.0

# ── vLLM (cluster inference server) ───────────────────────────────────────────
vllm

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=8.2.0
iniconfig
pygments
