# scratch/

One-off and throwaway scripts: local debugging harnesses, ad-hoc analyses,
AI-generated exploration code. Nothing here is part of the pipeline, nothing
here may be imported by `src/` or `scripts/`, and nothing here is expected to
keep working. Promote anything that becomes load-bearing into `scripts/` or
`src/` under the naming rules in CLAUDE.md.
