# ── API keys ──────────────────────────────────────────────────────────────────
OPENROUTER_API_KEY=your-key-here
OPENAI_API_KEY=your-key-here
HUGGINGFACE_TOKEN=your-key-here
TOGETHER_API_KEY=your-key-here

# ── Hugging Face publishing (the publish stage) ───────────────────────────────
# Namespace for published artifacts (datasets/adapters/eval results). Leave
# unset to publish under the token owner's account. The token needs WRITE
# permission either way; repos are created automatically. If HUGGINGFACE_TOKEN
# is unset, the publish stage falls back to the cached `hf auth login` token.
# HF_ORG=your-org-name
# Canonical HF dataset repo for the demo pool: data/ is only a cache, and any
# missing pool file is auto-fetched from here (src/data/hf_demo_pool.py).
# HF_DEMO_POOL_REPO=icrl-finetuning/2026-06-04-stwebagentbench-suitecrm-demos

# ── Benchmark web apps (also set in ST-WebAgentBench/.env) ────────────────────
# Required for live browser episodes. SuiteCRM is the primary app for easy-tier tasks.
WA_SUITECRM=http://localhost:8080/public
# GITLAB and SHOPPING_ADMIN are NOT optional: ST-WebAgentBench validates every
# site URL when it loads and refuses to start if any is unset — even when you
# only run SuiteCRM tasks (235-254). Point them at SuiteCRM if you have no
# GitLab/shopping instance; nothing in the easy tier dereferences them.
GITLAB=http://localhost:8080
SHOPPING_ADMIN=http://localhost:8080

# ── Paths (optional — setup_cluster.sh sets these in activate_icrl.sh) ────────
# STWEBAGENT_ROOT=/scratch/$USER/ST-WebAgentBench
# BROWSERGYM_ROOT=/scratch/$USER/BrowserGym

# ── W&B (optional) ────────────────────────────────────────────────────────────
# WANDB_API_KEY=your-key-here
# WANDB_ENTITY=your-entity-name
# WANDB_PROJECT=icrl-safety
