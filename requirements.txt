# Full icrl install (ML + browser benchmark + agentlab + vLLM).
# Cluster one-shot: bash scripts/setup_cluster.sh
#
# On Compute Canada, if typing_extensions resolves to the system copy (4.12.x):
#   pip install "typing_extensions>=4.13.0" --force-reinstall --no-deps

-r requirements-browser.txt

# ── Core ML stack ─────────────────────────────────────────────────────────────
torch==2.11.0
transformers>=4.41.0
peft>=0.11.1
trl>=0.9.4
accelerate>=0.30.0
bitsandbytes>=0.43.1
datasets>=2.19.0
huggingface-hub>=0.34.0
safetensors>=0.4.3
tokenizers>=0.21.0

# torch / transformers transitive (often missing with --no-deps cluster installs)
sympy
jinja2
networkx

# ── Scientific / data ─────────────────────────────────────────────────────────
scikit-learn>=1.4.2
scipy>=1.13.0
numpy>=1.26.4
pandas>=2.2.2
matplotlib>=3.9.0
seaborn>=0.13.2
joblib

# ── Config / logging ──────────────────────────────────────────────────────────
omegaconf>=2.3.0
hydra-core>=1.3.2
antlr4-python3-runtime==4.9.*
wandb>=0.17.0
jsonlines>=4.0.0

# ── Agent / browser infra ─────────────────────────────────────────────────────
# browsergym-core and stwebagentbench are installed as editable packages
# (pip install -e ~/BrowserGym/browsergym/core --no-deps etc.) — not listed here.
# browsergym-webarena is installed --no-deps so its deps must be listed explicitly.
browsergym-webarena
gymnasium
# playwright must be installed WITH deps (greenlet, pyee, etc.) — do not --no-deps
playwright
greenlet
pyee
pillow
nltk
# browsergym-core deps (from BrowserGym/browsergym/core/requirements.txt)
pyparsing
beautifulsoup4
lxml
mcp[cli]
# stwebagentbench deps (from ST-WebAgentBench/browsergym/stwebagentbench/requirements.txt)
PyPDF2
rapidfuzz
pydantic>=2.0
pydantic-core
typing-extensions>=4.13.0
typing_inspection

# ── agentlab and its transitive runtime deps ──────────────────────────────────
# Install agentlab with --no-deps if pip resolver hangs on gradio:
#   pip install agentlab --no-deps && pip install -r requirements.txt
agentlab>=0.1.0
gradio==5.31.0
ray[default]==2.49.1
anthropic
contexttimer
dask
distributed
ipython
litellm
python-slugify
tiktoken

# ── LLM API clients ───────────────────────────────────────────────────────────
openai>=1.30.0,<2
python-dotenv>=1.0.0

# ── vLLM (cluster inference server) ───────────────────────────────────────────
vllm
uvloop
fastapi
uvicorn
msgspec
prometheus_client
prometheus-fastapi-instrumentator
partial-json-parser
blake3
einops
compressed-tensors
cbor2
psutil
pyzmq
threadpoolctl
openai-harmony
model-hosting-container-standards
cloudpickle
diskcache==5.6.3
lark==1.2.2
llguidance
xgrammar
mistral_common
depyf
watchfiles
python-json-logger
ninja
pybase64
ijson
setproctitle
cachetools
py-cpuinfo
opentelemetry-sdk
opentelemetry-api
opentelemetry-exporter-otlp
opentelemetry-semantic-conventions

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=8.2.0
iniconfig
pygments
