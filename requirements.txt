# ── Core ML stack ─────────────────────────────────────────────────────────────
torch>=2.3.0
transformers>=4.41.0
peft>=0.11.1
trl>=0.9.4
accelerate>=0.30.0
bitsandbytes>=0.43.1
datasets>=2.19.0
huggingface-hub>=0.23.0

# ── Scientific / data ─────────────────────────────────────────────────────────
scikit-learn>=1.4.2
scipy>=1.13.0
numpy>=1.26.4
pandas>=2.2.2
matplotlib>=3.9.0
seaborn>=0.13.2
joblib

# ── Config / logging ──────────────────────────────────────────────────────────
omegaconf>=2.3.0
hydra-core>=1.3.2
wandb>=0.17.0
jsonlines>=4.0.0

# ── Agent / browser infra ─────────────────────────────────────────────────────
# browsergym-core and stwebagentbench are installed as editable packages
# (pip install -e ~/BrowserGym/browsergym/core --no-deps etc.) — not listed here.
# browsergym-webarena is installed --no-deps so its deps must be listed explicitly.
browsergym-webarena
gymnasium
# playwright must be installed WITH deps (greenlet, pyee, etc.) — do not --no-deps
playwright
greenlet
pyee
pillow
nltk
# browsergym-core deps (from BrowserGym/browsergym/core/requirements.txt)
pyparsing
beautifulsoup4
lxml
mcp[cli]
# stwebagentbench deps (from ST-WebAgentBench/browsergym/stwebagentbench/requirements.txt)
PyPDF2
rapidfuzz
pydantic>=2.0
pydantic-core
typing-extensions>=4.13.0
typing_inspection

# ── agentlab and its transitive runtime deps ──────────────────────────────────
# Install the whole block with --no-deps to avoid the gradio backtracking hang.
# gradio must be pinned — agentlab's >=5.5 constraint causes multi-hour resolver
# hangs if pip is allowed to resolve it freely.
agentlab>=0.1.0
gradio==5.31.0
ray[default]==2.49.1
anthropic
contexttimer
dask
distributed
ipython
litellm
python-slugify
tiktoken

# ── LLM API clients ───────────────────────────────────────────────────────────
openai>=1.30.0,<2
python-dotenv>=1.0.0

# ── vLLM (cluster inference server) ───────────────────────────────────────────
vllm

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=8.2.0
