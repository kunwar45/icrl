# ABOUTME: ODCV-Bench on Compute Canada: vendored executor + sandbox API and the Apptainer instance runner that replaces docker compose
# ABOUTME: Import src.environments.odcv.apptainer_sandbox; the vendored files carry their upstream commit in their headers
